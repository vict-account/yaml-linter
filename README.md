# yaml-linter

A small, zero-dependency YAML sanity linter for CI pipelines.

## Usage

```bash
node src/index.js path/to/file.yaml
```

Pull requests are automatically reviewed by Claude — see
`.github/workflows/pr-review.yml`.

## License

MIT
